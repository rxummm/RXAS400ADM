/**
 * Namespace: network
 * Purpose: Network status toast messages
 * Usage: $t('network.restored') / $t('network.disconnected')
 */
export default {
  network: {
    restored: 'Network connection restored',
    disconnected: 'Network connection lost',
  },
}
