-- V31: AS400 连接密码 AES-256-GCM 加密支持
-- 说明：
--   1. 新增/编辑服务器时，密码自动 AES 加密后存入 password_encrypt
--   2. 解密时自动兼容存量明文密码（解密失败则当作明文返回）
--   3. 生产环境必须设置环境变量 RXAS400_CRYPTO_KEY（生成方式: openssl rand -base64 32）
--   4. 存量明文密码在下次编辑服务器时自动加密，无需手动迁移