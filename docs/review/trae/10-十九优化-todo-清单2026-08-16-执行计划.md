---
title: "十九、优化 TODO 清单（2026-08-16 执行计划）"
---

# 十九、优化 TODO 清单（2026-08-16 执行计划）

> 基于 §十八 优化建议校准，结合 AGENTS.md 编码规范与项目门禁体系，按优先级分组。

### ✅ 已完成项（审计后已修复）

| # | 事项 | 章节 | 说明 |
|---|------|------|------|
| 1 | `@Transactional(rollbackFor = Exception.class)` 全量补齐 | §11.1 | 55 处已统一 + 门禁 `check-transactional.sh` |
| 2 | request.ts 方法签名重构（get/post/put/delete 消除 `as unknown as`） | §2.1 | 已改为 `instance.get<T>().then(r => r.data)` |
| 3 | 前端硬编码中文收敛 | §15.3 | P2-30 已修复 5 处残留 |
| 4 | 插槽行类型标注 144/144 闭环 | §2.1 | `check-frontend-slots.sh` 门禁 |
| 5 | 分层准绳门禁 | §7 | `check-layering.sh` R1/R2/R3 |
| 6 | i18n key 全对齐 | §15.1 | `check-i18n.mjs` 门禁 |

### A 组：立即实施（低风险、高收益，合计约 1-1.5 天）

| # | 优先级 | 事项 | 章节 | 预估工时 | 关联规范 |
|---|--------|------|------|----------|----------|
| **A1** | 🔴 P0 | **修复剩余 3 处 `as unknown as` 类型断言** | §2.1 | 0.5天 | AGENTS.md 类型安全 |
| | | `calendar/index.vue` L108: `as unknown as string[]` → `as string[]`（tm() 返回 any） | | | |
| | | `data/sysvals/index.vue` L158: `as unknown as Record<string, unknown>` → 直接 `row[f]`（SystemValue 已有 `[key: string]: unknown`） | | | |
| | | `i18n/index.ts` L28: `as unknown as { value: string }` → `i18n.global.locale = normalized`（vue-i18n v9 正确 API） | | | |
| **A2** | 🔴 P0 | **`ApiResponse.data` 标注 `T \| null`** | §5.2 | ✅ 已完成 | request.ts L21 已修正 |
| **A3** | 🟡 P1 | **前端内联 `style` 17 处收敛** | §8.4 | 0.25天 | AGENTS.md「硬编码红线」 |
| | | 静态 `style="..."` 8 处：5 处可用 `.mt16`/`.ml8` 既有工具类，3 处需新增 `.flex`/`.gap16`/`.mr6` 工具类 | | | |
| | | 动态 `:style` 9 处保留（依赖运行时数据，非硬编码） | | | |
| **A4** | 🟡 P1 | **TagsView `storeToRefs` 改造** | §4.2/§14.2 | 0.25天 | AGENTS.md Pinia 规范 |
| | | 消除 5 处 `visitedViews.value = tagsStore.visitedViews` 手动同步 | | | |
| | | 改为 `storeToRefs` 直接解构，保留局部过滤/排序逻辑 | | | |

### B 组：下个迭代（需小范围设计，合计约 4-5 天）

| # | 优先级 | 事项 | 章节 | 预估工时 |
|---|--------|------|------|----------|
| **B1** | 🔴 P0 | **MenuService VO 化：消除 `Map<String, Object>` 黑盒** | §2.2/§7-Action1 | 1-2天 |
| **B2** | 🟡 P1 | **SysRole 类型收敛 + menuType 常量抽取** | §2.1/§8.3 | 0.5天 |
| **B3** | 🟡 P1 | **CTE SQL 迁移到 XML Mapper** | §3.2/§7-Action3 | 1.5天 |
| **B4** | 🟢 P2 | **Service 接口化试点** | §8.1 | 1天 |
| **B5** | 🟢 P2 | **`bindRoles()` N+1 优化** | §8.6/§10.1 | 0.5天 |
| **B6** | 🟢 P2 | **`PermissionManageService.toMap()` → `PermissionVO`** | §8.3 | 0.5天 |

### C 组：纳入门禁/规范（防止回归）

| # | 优先级 | 事项 | 章节 | 预估工时 |
|---|--------|------|------|----------|
| **C1** | 🟡 P1 | **内联 `style` 纳入 lint 门禁** | §8.4 | 0.25天 |
| **C2** | 🟢 P2 | **i18n 文件按模块拆分** | §8.5/§15.4 | 1天 |
| **C3** | 🟢 P2 | **i18n 文件头部添加命名空间约定注释** | §15.4 | 0.1天 |

### D 组：暂缓（依赖外部条件）

| # | 事项 | 章节 | 说明 |
|---|------|------|------|
| **D1** | 动态事务开关 + AS400 部署流水线 | §12/§13 | 依赖 AS400 主数据源落地 |
| **D2** | 后端 `BusinessException` 全量 i18n | §8.2/§8.5 | 先覆盖高频错误码 |
| **D3** | `@EnableAsync` 事件异步化 | §11.3 | 当前缓存失效逻辑很轻 |

### E 组：文档维护

| # | 事项 | 说明 |
|---|------|------|
| **E1** | 审计文档站拆页同步 | A 组修复完成后重跑 `npm run split:trae` |
| **E2** | `AAA-Remark` 状态标记更新 | 修复后在对应 Remark 后追加「已修复」标记 |

### 执行顺序

```
第一轮（本周，1-1.5 天）：A1 → A3 → A4
第二轮（下迭代，4-5 天）：B1 → B2 → B3 → B4 → B5 → B6
门禁加固（伴随第二轮）：C1 → C2 → C3
```

**核心原则**：每次改动后跑 `bash scripts/verify-all.sh`；前端改动后跑 `npm run build`；后端改动后跑 `mvn -q test`。

---
