---
title: "一、直击痛点：阻碍新人理解的 Top 5 核心\"坏味道\""
---

# 一、直击痛点：阻碍新人理解的 Top 5 核心"坏味道"

| #   | 问题                                                             | 严重程度           | 影响范围        |
| --- | ---------------------------------------------------------------- | ------------------ | --------------- |
| 1   | **MenuService.java 超级巨石类 + `Map<String, Object>` 黑盒传输** | [阻碍理解/Blocker] | 菜单系统全链路  |
| 2   | **Service 层泛滥 `QueryWrapper`，业务与持久层严重耦合**          | [阻碍理解/Blocker] | 43 个 Java 文件 |
| 3   | **前端 `request.ts` 类型断言断裂，`as unknown as` 泛滥**         | [阻碍理解/Blocker] | 所有 API 调用链 |
| 4   | **`@Select` 注解内嵌复杂 CTE SQL，无 XML 管理**                  | [规范警告/Warning] | 多表联查        |
| 5   | **前端组件 inline 复杂类型，缺乏可复用领域模型**                 | [规范警告/Warning] | Vue 组件        |

> **AAA-Remark（2026-08-15 复核）**：Top 5 全部真实存在，逐一验证结果——① MenuService 有 10+ 个方法返回 `Map<String, Object>`（`toMenuMap`/`userMenuData` 等，见 §2.2/§7 备注）；② 全库 43 个 Java 文件、212 处 `QueryWrapper`/`LambdaQueryWrapper`（附录「50+」低估，见 §3.1 备注）；③ `as unknown as` 全库 8 处（request.ts 5 处 + calendar/sysvals/i18n 各 1，文档附录「4 处」低估）；④ `SysMenuMapper` L19 `@Select` 内嵌 CTE 属实、Mapper XML 为 0；⑤ 前端内联类型仍在（如 Users.vue `editData`），但插槽类型化专项已 100% 完成（144/144）。严重程度建议：①→已排期方向、②→已建门禁仅剩存量、③→建议最高优先级、④→低风险可排期、⑤→Info 级。→ **状态汇总（2026-08-16 回填）：① ✅ 已 VO 化（MenuVO/TabVO/UserMenuDataVO/RequestableMenuVO，见 08-16 §1.1 #5）；② ✅ 分层门禁已闭环（R2 白名单 2026-08-16 清零，任何 Entity 入参即失败）；③ ✅ 全库 0 处（request.ts 断言链已消除，仅剩 1 处注释）；④ ✅ 已迁 XML（SysMenuMapper.xml + R11 全库复核 + 2 处 GROUP BY 聚合下推）；⑤ ✅ 插槽类型化 147/147 全标注，内联类型经 U6 常量抽取收敛**

---
